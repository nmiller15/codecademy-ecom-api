'use strict';


/**
 * Remove item(s)
 * Remove one item from a cart by matching one or more product codes in the request body.
 *
 * userId Integer A unique number belonging to each user.
 * no response value expected for this operation
 **/
exports.delete_user_id_cart = function(userId) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Get user cart.
 * Retrieve the contents of a user's cart by matching a user id.
 *
 * userId Integer A unique number belonging to each user.
 * no response value expected for this operation
 **/
exports.get_user_id_cart = function(userId) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Add product to cart.
 * Add a product, identified by its id, to a cart, identified by matching user id.
 *
 * userId Integer A unique number belonging to each user.
 * no response value expected for this operation
 **/
exports.post_user_id_cart = function(userId) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

